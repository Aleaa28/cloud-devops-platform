apiVersion: v1
kind: Service
metadata:
  name: {{ include "infrastructure-health-api.fullname" . }}
  labels:
    {{- include "infrastructure-health-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infrastructure-health-api.selectorLabels" . | nindent 4 }}
