apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "infrastructure-health-api.fullname" . }}-grafana-dashboard
  labels:
    grafana_dashboard: "1"
data:
  infrastructure-health.json: |-
{{ .Files.Get "dashboards/infrastructure-health.json" | indent 4 }}
