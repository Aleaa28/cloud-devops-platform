apiVersion: v1
kind: Service
metadata:
  name: infrastructure-health-api
spec:
  selector:
    app: infrastructure-health-api
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
